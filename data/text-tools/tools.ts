export type TextTool = {
  slug: string;
  name: string;
  title: string;
  description: string;
  longDescription: string;
  primaryKeyword: string;
  category: "text-tools";
  howTo: string[];
  faqs: { question: string; answer: string }[];
};

export const textTools: TextTool[] = [
  {
    slug: "markdown-to-html",
    name: "Markdown to HTML",
    title: "Markdown to HTML Converter — Free Online Tool",
    description: "Convert Markdown to HTML instantly in your browser. Supports GFM, tables, code blocks and more. Free, no signup.",
    longDescription: "Markdown is a lightweight markup language used widely in documentation, README files, blog posts, and static site generators. This tool converts your Markdown source to clean HTML using the industry-standard marked library, which fully supports GitHub Flavoured Markdown (GFM), including tables, task lists, fenced code blocks, and strikethrough. The output can be previewed live in the built-in rendered preview panel, or downloaded as a standalone HTML file. Everything runs in your browser — your text is never sent to a server.",
    primaryKeyword: "markdown to html converter",
    category: "text-tools",
    howTo: [
      "Paste or type your Markdown source into the input textarea on the left.",
      "The HTML output is generated instantly. Click 'Preview' to see the rendered result.",
      "Copy the HTML to clipboard or download it as an .html file.",
    ],
    faqs: [
      { question: "What Markdown features are supported?", answer: "The converter uses the marked library with GitHub Flavoured Markdown (GFM) enabled. Supported features include headings, bold, italic, strikethrough, inline code, fenced code blocks, blockquotes, ordered and unordered lists, task lists (- [ ] and - [x]), tables, horizontal rules, and links with optional titles. HTML passthrough is also supported — raw HTML in the Markdown source is preserved in the output." },
      { question: "Does the output include a full HTML document wrapper?", answer: "The output from this converter is a fragment — just the HTML elements converted from your Markdown, without an outer <html>, <head>, or <body> tag. This is the most useful format for embedding the output into an existing HTML page or template. If you need a complete standalone HTML file, the downloaded .html file includes a minimal document wrapper with a charset meta tag." },
      { question: "Can I use this to preview README files?", answer: "Yes. GitHub and most Git hosting platforms render README.md files using GFM. This tool uses the same GFM standard, so the preview here closely matches what GitHub will display. Paste the contents of your README.md and use the Preview button to see how it will look before pushing it to your repository." },
      { question: "Why use Markdown instead of writing HTML directly?", answer: "Markdown is far faster to write than HTML for prose content. A heading is # Title instead of <h1>Title</h1>. Bold text is **word** instead of <strong>word</strong>. Markdown is also human-readable in its raw form, which makes it ideal for documentation that lives in version control alongside code. This converter is useful when you need to go from the Markdown source back to the HTML it represents — for example, when injecting content into a CMS or email template." },
      { question: "Is my text sent to a server?", answer: "No. The conversion runs entirely in your browser using the marked JavaScript library. Your text is never sent to any server or third-party service. You can safely use this tool with confidential documents." },
      { question: "What is the maximum input size?", answer: "There is no hard limit imposed by this tool — the conversion is limited only by your browser's JavaScript memory. In practice, Markdown documents up to several megabytes convert instantly. Extremely large documents (tens of megabytes) may be slow on lower-powered devices." },
    ],
  },
  {
    slug: "html-to-markdown",
    name: "HTML to Markdown",
    title: "HTML to Markdown Converter — Free Online Tool",
    description: "Convert HTML to clean Markdown instantly in your browser. Strips formatting and extracts readable Markdown. Free, no signup.",
    longDescription: "Converting HTML back to Markdown is useful when migrating content from a CMS to a static site generator, extracting readable text from web pages, or cleaning up pasted rich text. This tool uses the Turndown library to parse HTML and produce idiomatic Markdown. It handles headings, bold, italic, links, images, lists, code blocks, blockquotes, and tables. The resulting Markdown is clean and human-readable — ready to commit to a repository or paste into any Markdown editor.",
    primaryKeyword: "html to markdown converter",
    category: "text-tools",
    howTo: [
      "Paste the HTML code you want to convert into the input textarea.",
      "The Markdown output is generated instantly as you type or paste.",
      "Copy the Markdown to clipboard or download it as a .md file.",
    ],
    faqs: [
      { question: "What HTML elements are converted to Markdown?", answer: "The converter handles all common HTML elements: <h1>–<h6> become # headings, <strong> and <b> become **bold**, <em> and <i> become *italic*, <a> becomes [text](url), <img> becomes ![alt](src), <ul>/<ol>/<li> become Markdown lists, <pre><code> becomes fenced code blocks, <blockquote> becomes > quotes, and <table> becomes GitHub Flavoured Markdown tables. Unrecognised elements are either converted to their text content or preserved as raw HTML." },
      { question: "Can I convert a full web page to Markdown?", answer: "Yes. You can paste the entire HTML source of a page — including the <html>, <head>, and <body> tags. The converter will process the full document, though the <head> section (meta tags, scripts, styles) will be stripped since it has no Markdown equivalent. For best results with web pages, paste just the content area (the article body or main tag) rather than the full document." },
      { question: "Does the converter preserve links and images?", answer: "Yes. HTML links (<a href='...'>) are converted to [link text](url) Markdown syntax. Images (<img src='...' alt='...'>) are converted to ![alt text](url). Both inline and reference-style link formats are supported in the output. Image sizes and CSS classes are discarded since Markdown does not support them." },
      { question: "What happens to inline CSS and style attributes?", answer: "Inline styles, CSS classes, and data attributes are stripped from the output. Markdown does not have a concept of styling beyond the limited formatting it natively supports (bold, italic, code). If your HTML relies heavily on CSS for its appearance, the Markdown output will reflect only the structural and semantic content, not the visual presentation." },
      { question: "Is my HTML sent to a server?", answer: "No. The conversion runs entirely in your browser using the Turndown JavaScript library. Your HTML is never sent to any server or third-party service. This makes the tool safe to use with internal company HTML, proprietary content, or any confidential material." },
      { question: "Why would I need to convert HTML back to Markdown?", answer: "Common use cases include: migrating blog posts from a WordPress CMS (which stores HTML) to a Markdown-based static site like Hugo or Astro; converting email HTML to a readable format; extracting content from web pages for documentation purposes; and cleaning up pasted rich text from a word processor (which typically pastes as HTML in web applications). Markdown is also widely used in AI chatbot prompts, where plain-text formatting is preferred over raw HTML." },
    ],
  },
  {
    slug: "csv-to-json",
    name: "CSV to JSON",
    title: "CSV to JSON Converter — Free Online Tool",
    description: "Convert CSV data to JSON format online. Supports headers, custom delimiters and file upload. Free, browser-based.",
    longDescription: "CSV (Comma-Separated Values) is a universal data exchange format, but JSON is the native format of web APIs and JavaScript applications. This tool converts CSV data to a JSON array of objects, using the first row of the CSV as the object keys. It uses the robust PapaParse library which handles edge cases including quoted fields containing commas, multi-line values, and different line endings. You can either paste CSV text directly or upload a .csv file.",
    primaryKeyword: "csv to json converter",
    category: "text-tools",
    howTo: [
      "Paste your CSV data into the input area, or upload a .csv file using the file button.",
      "Click 'Convert to JSON' to convert the CSV to a JSON array.",
      "Copy the JSON to clipboard or download it as a .json file.",
    ],
    faqs: [
      { question: "How does the converter handle the CSV header row?", answer: "The first row of the CSV is treated as the header row and becomes the keys of each JSON object. For example, a CSV with columns 'Name,Age,City' produces JSON objects like {\"Name\": \"Alice\", \"Age\": \"30\", \"City\": \"London\"}. All values in the JSON are strings unless you post-process them. If your CSV has no header row, the output will use the first data row as keys, which is usually undesirable — add a header row first." },
      { question: "What delimiters are supported besides comma?", answer: "PapaParse automatically detects the delimiter in your CSV. If your file uses tabs (TSV), semicolons (common in European locales where commas are used as decimal separators), pipes, or other characters, the library will detect and use the correct delimiter. You do not need to configure this manually." },
      { question: "How are quoted fields and fields containing commas handled?", answer: "RFC 4180 CSV quoting is fully supported. Fields that contain commas, double quotes, or newlines should be enclosed in double quotes in the CSV source. An embedded double quote is escaped by doubling it (\"\"). PapaParse correctly parses all of these cases. For example, \"Smith, John\",\"30\" is correctly parsed as two fields: 'Smith, John' and '30'." },
      { question: "What is the maximum file size for CSV upload?", answer: "There is no hard limit imposed by this tool. The conversion is handled entirely in the browser. Practical performance depends on your device — CSV files up to 10MB convert in well under a second on modern hardware. Very large files (100MB+) may cause the browser tab to become temporarily unresponsive during parsing." },
      { question: "Are all values in the JSON output strings?", answer: "Yes, by default all values are output as JSON strings, even numbers and booleans, because CSV has no type system. If you need typed values (numbers as numbers, true/false as booleans), you will need to post-process the JSON or use a tool that performs type inference. This conservative approach avoids data loss — for example, a leading zero in a zip code ('01234') would be lost if parsed as a number." },
      { question: "Is the conversion done in my browser or on a server?", answer: "The conversion runs entirely in your browser using the PapaParse library. Your data is never uploaded to any server. This is important for CSV files that may contain personally identifiable information (PII), financial data, or other sensitive content." },
    ],
  },
  {
    slug: "json-to-csv",
    name: "JSON to CSV",
    title: "JSON to CSV Converter — Free Online Tool",
    description: "Convert JSON arrays to CSV format online. Handles nested objects and arrays. Free, browser-based.",
    longDescription: "JSON is the standard format for web APIs, but CSV is widely used in spreadsheets, databases, and data analysis tools. This tool converts a JSON array of objects to CSV format, using the object keys as the CSV header row. It uses PapaParse's unparse function which handles all RFC 4180 escaping correctly — fields containing commas or double quotes are automatically quoted. If the JSON contains nested objects or arrays, they are serialised to strings in the corresponding CSV cell.",
    primaryKeyword: "json to csv converter",
    category: "text-tools",
    howTo: [
      "Paste your JSON array into the input textarea.",
      "Click 'Convert to CSV' to generate the CSV output.",
      "Copy the CSV to clipboard or download it as a .csv file.",
    ],
    faqs: [
      { question: "What JSON structure is expected as input?", answer: "The tool expects a JSON array of objects, where each object represents a row and its keys represent the column names. For example: [{\"Name\": \"Alice\", \"Age\": 30}, {\"Name\": \"Bob\", \"Age\": 25}]. If the input is a single JSON object (not an array), it is automatically wrapped in an array before conversion. Other JSON structures (arrays of arrays, primitive values) are not supported and will produce an error." },
      { question: "How are inconsistent objects handled?", answer: "If the objects in the array have different keys (some rows have columns that others lack), the CSV header row will include all unique keys found across all objects. Rows that are missing a key will have an empty cell in the corresponding column. This ensures no data is lost, though the resulting CSV may have sparse rows." },
      { question: "What happens to nested objects and arrays in the JSON?", answer: "JSON supports nesting — an object value can itself be an object or array. CSV is flat and does not support nesting. Nested values are serialised to their JSON string representation and placed in the CSV cell as a string (quoted if necessary). For example, a nested array [1, 2, 3] becomes the string '[1,2,3]' in the CSV cell." },
      { question: "How are special characters handled in the output?", answer: "The PapaParse unparse function handles RFC 4180 escaping automatically. If a cell value contains a comma, double quote, newline, or carriage return, the entire value is wrapped in double quotes in the output. Embedded double quotes are escaped by doubling them. This ensures the CSV output is correctly parseable by all compliant CSV parsers." },
      { question: "Can I control the delimiter or quote character?", answer: "This tool outputs standard comma-delimited CSV with double-quote escaping, which is the most universally compatible format. For tab-delimited output (TSV) or other delimiters, the tool does not currently offer delimiter selection. You can manually replace commas with tabs in the output if needed, being careful not to replace commas inside quoted fields." },
      { question: "Is the conversion done in my browser or on a server?", answer: "The conversion runs entirely in your browser using the PapaParse library. Your JSON data is never uploaded to any server, making this tool safe for confidential API response data, customer records, or other sensitive information." },
    ],
  },
  {
    slug: "xml-to-json",
    name: "XML to JSON",
    title: "XML to JSON Converter — Free Online Tool",
    description: "Convert XML to JSON format online. Handles attributes, nested elements and CDATA. Free, browser-based.",
    longDescription: "XML was the dominant data interchange format before JSON took over in web APIs. This tool converts XML documents to JSON using the browser's built-in DOMParser to parse the XML, then recursively walks the DOM tree to build a JavaScript object. Attributes are included as properties prefixed with '@', text content is stored as a '#text' property, and nested elements become nested JSON objects. The output is prettily formatted and ready to copy or download.",
    primaryKeyword: "xml to json converter",
    category: "text-tools",
    howTo: [
      "Paste your XML document into the input textarea.",
      "Click 'Convert to JSON' to parse the XML and generate the JSON output.",
      "Copy the JSON to clipboard or download it as a .json file.",
    ],
    faqs: [
      { question: "How are XML attributes converted to JSON?", answer: "XML attributes are included in the JSON output as properties prefixed with the '@' character. For example, <person id='1' name='Alice'> is converted to {\"@id\": \"1\", \"@name\": \"Alice\", ...}. This convention is widely used in XML-to-JSON converters and clearly distinguishes attributes from child element properties." },
      { question: "How are text nodes and mixed content handled?", answer: "If an element contains only text (no child elements or attributes), the JSON value is the text string itself. If an element has both text content and child elements or attributes, the text content is stored as a '#text' property alongside the other properties. CDATA sections are treated as text content." },
      { question: "What happens when there are multiple sibling elements with the same tag name?", answer: "When multiple sibling elements share the same tag name, they are collected into a JSON array under that tag name. For example, <items><item>a</item><item>b</item></items> is converted to {\"items\": {\"item\": [\"a\", \"b\"]}}. This is the most natural JSON representation of repeated XML elements." },
      { question: "Is the XML namespace preserved in the JSON output?", answer: "XML namespace prefixes are preserved in the property names (e.g., 'ns:element' becomes an 'ns:element' key in JSON), but namespace URI declarations (xmlns attributes) are included as regular '@xmlns' properties. Full namespace resolution is not performed — the output reflects the raw XML structure." },
      { question: "What XML features are not supported?", answer: "Processing instructions (<?...?>) and XML comments (<!-- -->) are not included in the JSON output. DOCTYPE declarations are ignored. The XML declaration (<?xml version='1.0'?>) is also excluded. Only element nodes, attribute nodes, and text/CDATA nodes are converted." },
      { question: "Is the conversion done in my browser or on a server?", answer: "The conversion runs entirely in your browser using the built-in DOMParser API. Your XML is never sent to any server. This is important for XML files that may contain proprietary business data, configuration files, or other sensitive content." },
    ],
  },
  {
    slug: "json-to-xml",
    name: "JSON to XML",
    title: "JSON to XML Converter — Free Online Tool",
    description: "Convert JSON to XML format online. Wraps in a root element, handles arrays and nested objects. Free, browser-based.",
    longDescription: "JSON is the preferred format for modern web APIs, but many enterprise systems, legacy applications, and SOAP-based services require XML. This tool converts a JSON object or array to a valid XML document, wrapping the output in a configurable root element name. Arrays become repeated XML elements with the same tag. Nested objects become nested elements. All values are encoded to ensure valid XML output.",
    primaryKeyword: "json to xml converter",
    category: "text-tools",
    howTo: [
      "Paste your JSON object or array into the input textarea.",
      "Click 'Convert to XML' to generate the XML document.",
      "Copy the XML to clipboard or download it as a .xml file.",
    ],
    faqs: [
      { question: "What root element name is used for the output XML?", answer: "The output XML is wrapped in a <root> element by default. This is necessary because XML requires exactly one root element. If your JSON is an object with a single top-level key, that key becomes the root element name. For arrays, the items are wrapped in <item> elements inside the <root> element." },
      { question: "How are JSON arrays converted to XML?", answer: "JSON arrays are converted to repeated XML elements. For example, {\"items\": [\"a\", \"b\", \"c\"]} becomes <items><item>a</item><item>b</item><item>c</item></items>. If the JSON root is an array, each element becomes an <item> element inside the <root> wrapper." },
      { question: "How are JSON keys that are invalid XML element names handled?", answer: "XML element names cannot start with a number or contain spaces or special characters. If a JSON key would produce an invalid XML element name (e.g., '1key' or 'my key'), the converter prefixes it with 'key_' or replaces spaces with underscores to produce a valid XML element name." },
      { question: "Are null values included in the XML output?", answer: "JSON null values are represented as empty XML elements. For example, {\"name\": null} becomes <name/>. This preserves the presence of the field in the output while correctly representing the absence of a value." },
      { question: "Does the output include an XML declaration?", answer: "Yes. The output includes the standard XML declaration <?xml version='1.0' encoding='UTF-8'?> at the top. The encoding is always UTF-8, which is the standard encoding for XML documents and matches the Unicode content that JavaScript strings produce." },
      { question: "Is the conversion done in my browser or on a server?", answer: "The conversion runs entirely in your browser using pure JavaScript string manipulation. No data is sent to any server. This makes the tool safe for converting API responses, configuration data, or any other sensitive JSON content." },
    ],
  },
  {
    slug: "csv-to-xml",
    name: "CSV to XML",
    title: "CSV to XML Converter — Free Online Tool",
    description: "Convert CSV data to XML format online. Uses header row as element names. Free, browser-based.",
    longDescription: "This tool converts CSV data to XML format, using the CSV header row as the XML element names for each column value. Each CSV row becomes an XML <record> element containing child elements for each column. The output is a well-formed XML document wrapped in a <records> root element. PapaParse handles robust CSV parsing including quoted fields and varied delimiters.",
    primaryKeyword: "csv to xml converter",
    category: "text-tools",
    howTo: [
      "Paste your CSV data (with a header row) into the input textarea.",
      "Click 'Convert to XML' to generate the XML document.",
      "Copy the XML to clipboard or download it as a .xml file.",
    ],
    faqs: [
      { question: "What XML structure does the output use?", answer: "The output wraps all rows in a <records> root element. Each CSV row becomes a <record> element. Each cell value in the row becomes a child element whose tag name is derived from the CSV column header. For example, a row with Name='Alice' and Age='30' produces <record><Name>Alice</Name><Age>30</Age></record>." },
      { question: "What happens if a header contains characters invalid in XML element names?", answer: "XML element names cannot contain spaces, start with numbers, or use most special characters. If a CSV column header would produce an invalid XML element name, spaces are replaced with underscores and invalid leading characters are prefixed with 'col_'. For example, a header '1st Name' becomes the element <col_1st_Name>." },
      { question: "How are special XML characters in values handled?", answer: "All cell values are XML-escaped before being placed in the output. The characters <, >, &, ', and \" are replaced with their XML entity references (&lt;, &gt;, &amp;, &apos;, &quot;). This ensures the output is always valid, well-formed XML regardless of the content of the CSV cells." },
      { question: "What delimiters does the CSV parser support?", answer: "PapaParse automatically detects the delimiter in your CSV. Comma, tab, semicolon, and pipe delimiters are all detected automatically. You do not need to specify the delimiter — the library analyses the file and selects the most likely delimiter." },
      { question: "Is a header row required?", answer: "Yes. The first row of the CSV is used to generate the XML element names for each column. If your CSV has no header row, the first data row will be used as element names, which is usually incorrect. Add a descriptive header row to your CSV before converting." },
      { question: "Is the conversion done in my browser or on a server?", answer: "The conversion runs entirely in your browser. No data is sent to any server. PapaParse parses the CSV and the XML is built using pure JavaScript string operations." },
    ],
  },
  {
    slug: "xml-to-csv",
    name: "XML to CSV",
    title: "XML to CSV Converter — Free Online Tool",
    description: "Convert XML to CSV format online. Extracts repeated elements as rows. Free, browser-based.",
    longDescription: "Converting XML to CSV is useful for importing XML data exports into spreadsheets or databases. This tool parses an XML document using the browser's DOMParser, identifies the repeated element (the most common child of the root), and extracts its child elements as CSV columns. This works well for XML exports from databases or ERP systems where the structure is a list of records.",
    primaryKeyword: "xml to csv converter",
    category: "text-tools",
    howTo: [
      "Paste your XML document into the input textarea.",
      "Click 'Convert to CSV' to extract the data as CSV rows.",
      "Copy the CSV to clipboard or download it as a .csv file.",
    ],
    faqs: [
      { question: "What XML structure is required for conversion?", answer: "The tool works best with XML that represents a list of records — a root element containing multiple repeated child elements of the same type, each with the same child elements as columns. For example: <records><record><name>Alice</name><age>30</age></record><record>...</record></records>. The tool identifies the first repeated element as the row type and uses its child element names as CSV column headers." },
      { question: "How are XML attributes handled?", answer: "XML attributes on the record elements are included as CSV columns, with the column name being the attribute name. Both child element content and attribute values from the same record element are included in the same CSV row." },
      { question: "What happens with nested XML elements?", answer: "The converter extracts only the direct children of each record element. If a child element itself contains further nested elements, its text content (the concatenated text of all its descendants) is used as the cell value. Deep nesting is not fully supported — for complex nested XML, consider using the XML to JSON converter first and then processing the JSON." },
      { question: "Can I convert an XML file with a single record?", answer: "Yes, but the output will have only one data row (plus the header row). The tool identifies the record type from the most common repeated child element. A single-record XML will still produce valid CSV output." },
      { question: "How are XML special characters in text content handled?", answer: "XML entity references (&lt;, &gt;, &amp;, &apos;, &quot;) are automatically decoded by the DOMParser before the text is placed in the CSV output. So &amp; in XML becomes & in the CSV, which is the correct behaviour." },
      { question: "Is the conversion done in my browser or on a server?", answer: "The conversion runs entirely in your browser using the built-in DOMParser API and PapaParse. No data is sent to any server." },
    ],
  },
  {
    slug: "yaml-to-json",
    name: "YAML to JSON",
    title: "YAML to JSON Converter — Free Online Tool",
    description: "Convert YAML to JSON format online. Handles all YAML types including anchors and aliases. Free, browser-based.",
    longDescription: "YAML (YAML Ain't Markup Language) is a human-friendly data serialisation format widely used in configuration files — Docker Compose, Kubernetes manifests, GitHub Actions workflows, and many CI/CD systems use YAML. This tool converts YAML to JSON using the js-yaml library, which fully supports YAML 1.2 including anchors (&), aliases (*), multi-line strings, and all native YAML types. The JSON output is prettily formatted and ready to use.",
    primaryKeyword: "yaml to json converter",
    category: "text-tools",
    howTo: [
      "Paste your YAML document into the input textarea.",
      "Click 'Convert to JSON' to parse the YAML and generate the JSON output.",
      "Copy the JSON to clipboard or download it as a .json file.",
    ],
    faqs: [
      { question: "What YAML features are supported?", answer: "The js-yaml library supports the full YAML 1.2 specification including: scalars (strings, numbers, booleans, null), sequences (lists), mappings (objects), multi-line string literals (|) and folded strings (>), anchors (&name) and aliases (*name), explicit type tags (!!str, !!int, !!float, !!bool, !!null, !!seq, !!map), and comments (which are stripped from the JSON output as JSON does not support comments)." },
      { question: "How are YAML types mapped to JSON types?", answer: "YAML strings become JSON strings. YAML integers and floats become JSON numbers. YAML true/false become JSON true/false. YAML null and ~ become JSON null. YAML sequences become JSON arrays. YAML mappings become JSON objects. YAML timestamps (dates) are converted to ISO 8601 strings in the JSON output." },
      { question: "What are YAML anchors and aliases, and are they supported?", answer: "Anchors (&name) mark a node for reuse, and aliases (*name) reference a previously defined anchor. This allows you to avoid repetition in YAML — define a block once and reference it multiple times. The js-yaml library fully resolves anchors and aliases before converting to JSON, so the output JSON contains the fully expanded value at each alias location." },
      { question: "Can I convert Kubernetes or Docker Compose YAML files?", answer: "Yes. Kubernetes manifests and Docker Compose files are standard YAML documents. Paste the content of any .yaml or .yml file and it will be converted to JSON. Note that Kubernetes manifests often use multi-document YAML (multiple documents separated by ---). This tool processes only the first document if multiple are present." },
      { question: "What happens to YAML comments?", answer: "YAML comments (lines starting with # or inline # text) are stripped during parsing and do not appear in the JSON output. JSON does not support comments, so this is unavoidable. If you need to preserve comments, consider adding them as dedicated string fields in your YAML before converting." },
      { question: "Is the conversion done in my browser or on a server?", answer: "The conversion runs entirely in your browser using the js-yaml library. No data is sent to any server. This is important for YAML files that may contain API keys, credentials, connection strings, or other secrets in your configuration files." },
    ],
  },
  {
    slug: "json-to-yaml",
    name: "JSON to YAML",
    title: "JSON to YAML Converter — Free Online Tool",
    description: "Convert JSON to YAML format online. Produces clean, readable YAML from any JSON object. Free, browser-based.",
    longDescription: "YAML is more human-readable than JSON — it uses indentation instead of brackets, does not require quotes for most strings, and supports comments. This tool converts any valid JSON to idiomatic YAML using the js-yaml library. The output uses 2-space indentation, avoids unnecessary quoting, and produces clean YAML ready to use as a configuration file. Arrays are rendered using the YAML block sequence style for readability.",
    primaryKeyword: "json to yaml converter",
    category: "text-tools",
    howTo: [
      "Paste your JSON object or array into the input textarea.",
      "Click 'Convert to YAML' to generate the YAML output.",
      "Copy the YAML to clipboard or download it as a .yaml file.",
    ],
    faqs: [
      { question: "How are JSON types mapped to YAML types?", answer: "JSON strings become YAML strings (quoted only when necessary for disambiguation — e.g., the string 'true' is quoted as 'true' to prevent it being interpreted as a YAML boolean). JSON numbers become unquoted YAML integers or floats. JSON true/false become YAML true/false. JSON null becomes YAML null. JSON arrays become YAML block sequences. JSON objects become YAML block mappings." },
      { question: "Are string values always quoted in the YAML output?", answer: "No. js-yaml only quotes strings when quoting is required to avoid ambiguity. Simple strings containing only alphanumeric characters, hyphens, underscores, and spaces are not quoted. Strings that could be misinterpreted as other types (e.g., 'true', 'null', '1.5', '2024-01-01') are automatically quoted to preserve their string type." },
      { question: "Does the YAML output support comments?", answer: "No. JSON does not have comments, so there is no information to convert into YAML comments. The output YAML contains only the data from the JSON input, with no comments. You can manually add comments to the YAML output after downloading it." },
      { question: "What indentation level is used in the YAML output?", answer: "The output uses 2-space indentation, which is the most common convention for YAML configuration files. This is consistent with Kubernetes manifests, Docker Compose files, GitHub Actions workflows, and most other YAML-based configuration systems." },
      { question: "Can I use this to create Kubernetes manifests from JSON?", answer: "Yes, to a degree. Kubernetes API responses are JSON (when queried with kubectl get -o json). You can convert these to YAML format for storage in version control. However, the converter produces semantically equivalent YAML, not necessarily the minimal manifest format used when writing Kubernetes configs by hand. You may want to remove fields like status and resourceVersion from the JSON before converting." },
      { question: "Is the conversion done in my browser or on a server?", answer: "The conversion runs entirely in your browser using the js-yaml library. No data is sent to any server, which is important for JSON content that may contain API keys, tokens, or other sensitive configuration data." },
    ],
  },
  {
    slug: "csv-viewer",
    name: "CSV Viewer",
    title: "CSV Viewer — View CSV Files as a Table Online",
    description: "View CSV files as an interactive HTML table online. Upload a file or paste CSV text. Free, browser-based.",
    longDescription: "Visualising CSV data as a table makes it much easier to understand the structure, spot errors, and verify row and column counts. This tool renders your CSV data as a formatted HTML table in the browser, with the header row highlighted and row counts displayed. You can paste CSV text directly or upload a .csv file. The rendering is instant and handles large files well. No data is ever uploaded to a server.",
    primaryKeyword: "csv viewer online",
    category: "text-tools",
    howTo: [
      "Paste CSV text into the input area, or click 'Upload CSV' to open a .csv file.",
      "The CSV is rendered as a formatted table instantly.",
      "Scroll horizontally for wide tables. Row and column counts are shown above the table.",
    ],
    faqs: [
      { question: "What delimiters are supported?", answer: "PapaParse automatically detects the delimiter. Comma, tab (TSV), semicolon, and pipe delimiters are all handled automatically. You do not need to specify the delimiter — the library analyses the input and selects the most likely delimiter based on the file content." },
      { question: "Is the first row treated as a header?", answer: "Yes. The first row of the CSV is used as the table header row, displayed with a distinct background style. If your CSV has no header row, the first data row will appear as the header, which may not be what you want. If that happens, manually add a header row to your CSV before pasting." },
      { question: "How large a CSV file can I view?", answer: "The tool can handle large CSV files, but rendering a very large table in the browser DOM can be slow. Files with up to a few thousand rows render near-instantly. Files with tens of thousands of rows may take a moment and produce a large HTML table that requires scrolling. For very large files, consider using a desktop tool like LibreOffice Calc." },
      { question: "Can I sort or filter the table?", answer: "This is a read-only viewer — it does not support interactive sorting, filtering, or column resizing. For interactive spreadsheet-style features in the browser, use Google Sheets or import the CSV into LibreOffice Calc. This tool is designed for quickly visualising the structure and content of a CSV file." },
      { question: "How are special characters displayed?", answer: "CSV values are HTML-escaped before being displayed in the table, so characters like <, >, and & are displayed correctly rather than being interpreted as HTML. Newlines within quoted CSV fields are preserved in the table cell." },
      { question: "Is my data sent to a server?", answer: "No. The CSV is parsed and rendered entirely in your browser using PapaParse and React. No data is uploaded to any server. This makes the tool safe for viewing CSV files containing personal data, financial records, or other sensitive information." },
    ],
  },
  {
    slug: "word-counter",
    name: "Word Counter",
    title: "Word Counter Online — Count Words, Characters and More",
    description: "Count words, characters, sentences, paragraphs and reading time as you type. Free, instant, browser-based.",
    longDescription: "This tool provides real-time text statistics as you type or paste text, with no need to click a button. It counts words, characters (with and without spaces), sentences, paragraphs, and estimates reading time based on an average reading speed of 200 words per minute. These statistics are useful for writers staying within word count limits, content marketers optimising for SEO, students writing essays with length requirements, and anyone who needs to know the length of their text.",
    primaryKeyword: "word counter online",
    category: "text-tools",
    howTo: [
      "Paste or type your text into the textarea below.",
      "Word count, character count, sentences, paragraphs and reading time update instantly as you type.",
      "No button press needed — statistics are always current.",
    ],
    faqs: [
      { question: "How is 'word' defined?", answer: "A word is defined as a sequence of non-whitespace characters. Contractions (don't, can't) count as one word. Hyphenated words (well-known) count as one word. Numbers and punctuation-only tokens (e.g., '---') count as words if they are surrounded by whitespace. This matches the definition used by most word processors including Microsoft Word and Google Docs." },
      { question: "How is 'sentence' counted?", answer: "Sentences are counted by splitting on sentence-ending punctuation: full stops (.), exclamation marks (!), and question marks (?). Abbreviations with periods (e.g., 'Dr.', 'etc.') may cause slight over-counting. The count is a practical estimate rather than a linguistically precise count." },
      { question: "How is reading time calculated?", answer: "Reading time is calculated using an average adult reading speed of 200 words per minute, which is a commonly cited figure for general text. For technical or dense academic text, actual reading time will be longer. For simple prose, it may be shorter. The estimate is displayed as minutes and seconds." },
      { question: "What is the difference between 'Characters' and 'Characters (no spaces)'?", answer: "Characters counts every character including spaces, newlines, and punctuation. Characters (no spaces) counts only characters that are not whitespace — every letter, number, and punctuation mark, but not spaces or line breaks. The 'no spaces' count is more useful for platform character limits that exclude spaces, such as some SMS systems or display width calculations." },
      { question: "Can I count words in languages other than English?", answer: "Yes. The word counter splits on whitespace, which works for all languages that use spaces between words — including French, German, Spanish, Polish, and other European languages. Languages that do not use spaces between words (Chinese, Japanese, Thai) will not count correctly — the entire text will appear as one 'word'." },
      { question: "Is there a character or word limit?", answer: "There is no limit imposed by the tool. Large texts (hundreds of thousands of words) are handled without issue, though very large inputs may have a barely noticeable delay before statistics update. The update is debounced slightly to maintain UI responsiveness during fast typing." },
    ],
  },
  {
    slug: "text-case-converter",
    name: "Text Case Converter",
    title: "Text Case Converter — UPPERCASE, lowercase, camelCase & More",
    description: "Convert text between UPPERCASE, lowercase, Title Case, camelCase, snake_case, kebab-case and more. Free, instant.",
    longDescription: "Changing the case of text is a frequent task for developers, writers, and content creators. This tool converts text between 8 different case styles instantly: UPPERCASE, lowercase, Title Case, Sentence case, camelCase, PascalCase, snake_case, and kebab-case. Each conversion is one click — no button press required after selecting the style. The result can be copied to clipboard instantly.",
    primaryKeyword: "text case converter",
    category: "text-tools",
    howTo: [
      "Paste or type your text into the input textarea.",
      "Click any of the conversion buttons (UPPERCASE, camelCase, etc.).",
      "The converted text appears in the output area. Click 'Copy' to copy it to clipboard.",
    ],
    faqs: [
      { question: "What case styles are supported?", answer: "The tool supports 8 case styles: UPPERCASE (all letters capitalised), lowercase (all letters in lower case), Title Case (first letter of each word capitalised), Sentence case (only the first letter of the first word capitalised), camelCase (first word lowercase, subsequent words capitalised, no spaces), PascalCase (all words capitalised, no spaces), snake_case (all lowercase, spaces replaced with underscores), and kebab-case (all lowercase, spaces replaced with hyphens)." },
      { question: "How does Title Case handle small words?", answer: "This tool uses a simple Title Case implementation that capitalises the first letter of every word including small words like 'a', 'an', 'the', 'in', 'of'. For a typographically correct Title Case that follows the Chicago Manual of Style rules (which leaves prepositions and articles in lowercase unless they start the title), the simple implementation used here may not be sufficient — a dedicated text editing tool would handle those nuances." },
      { question: "How does camelCase handle punctuation?", answer: "For camelCase and PascalCase conversions, the input text is split on whitespace and most non-alphanumeric characters (hyphens, underscores, punctuation). Each resulting word is capitalised (or left lowercase for the first word in camelCase) and the words are joined without spaces. Numbers are preserved in place." },
      { question: "What does snake_case do with existing punctuation?", answer: "For snake_case and kebab-case, the text is first converted to lowercase. All whitespace and common separator characters (hyphens, underscores) are replaced with a single underscore or hyphen respectively. Other punctuation is removed. This makes the output suitable for use as a variable name or URL slug." },
      { question: "Can I convert code identifiers?", answer: "Yes. The case converter works well for converting between programming naming conventions — for example, from PascalCase (class names in C#) to snake_case (variable names in Python) or kebab-case (CSS class names). Paste the identifier, click the target convention, and copy the result." },
      { question: "Is there a length limit for the input?", answer: "There is no practical limit. All conversions are pure JavaScript string operations that run in under a millisecond for typical inputs. Very long texts (millions of characters) will still convert quickly, though the textarea may be slow to render at such sizes." },
    ],
  },
  {
    slug: "remove-duplicate-lines",
    name: "Remove Duplicate Lines",
    title: "Remove Duplicate Lines from Text Online",
    description: "Remove duplicate lines from text online. Case-sensitive or insensitive. Free, browser-based.",
    longDescription: "Removing duplicate lines from lists, log files, code, or any text is a common data cleaning task. This tool filters out duplicate lines, keeping only the first occurrence of each unique line. Options include case-sensitive or case-insensitive comparison, and automatic whitespace trimming. After deduplication, the number of removed duplicates is shown so you can verify the result.",
    primaryKeyword: "remove duplicate lines online",
    category: "text-tools",
    howTo: [
      "Paste your text with duplicate lines into the input textarea.",
      "Choose whether the comparison is case-sensitive and whether to trim whitespace, then click 'Remove Duplicates'.",
      "The deduplicated text appears in the output with a count of removed lines.",
    ],
    faqs: [
      { question: "What constitutes a 'duplicate' line?", answer: "A line is a duplicate if it is identical to a previously seen line, taking into account the case-sensitivity and whitespace-trimming settings. By default, comparison is case-insensitive and leading/trailing whitespace is trimmed before comparison. So '  Hello  ' and 'hello' would be treated as duplicates with default settings. Only the first occurrence of each unique line is kept." },
      { question: "Does the tool preserve the original order of lines?", answer: "Yes. The first occurrence of each unique line is kept in its original position. Subsequent duplicates are removed. The output order matches the input order — the tool does not sort the lines." },
      { question: "What does 'trim whitespace' do?", answer: "When 'Trim whitespace' is enabled (the default), leading and trailing spaces and tabs are removed from each line before comparing. This means '  Hello' and 'Hello  ' are treated as the same line. The trimmed version is what appears in the output. Disable this option if you need to preserve whitespace differences between lines." },
      { question: "What is 'case-sensitive' comparison?", answer: "When case-sensitive comparison is disabled (the default), 'Hello', 'HELLO', and 'hello' are all treated as the same line. When case-sensitive is enabled, they are treated as three different lines. Enable case-sensitive for deduplicating code, file paths, or other content where case is significant." },
      { question: "Can I deduplicate a list with thousands of lines?", answer: "Yes. The deduplication algorithm uses a JavaScript Set for O(n) performance, which is efficient even for very large inputs. A list of 100,000 lines deduplicates in milliseconds in the browser." },
      { question: "Is my text sent to a server?", answer: "No. All processing runs entirely in your browser using pure JavaScript. No data is sent to any server, which is important for log files, email lists, or other sensitive text content." },
    ],
  },
  {
    slug: "sort-lines",
    name: "Sort Lines",
    title: "Sort Lines of Text Online — Alphabetical or Reverse",
    description: "Sort lines of text alphabetically or in reverse order online. Case-sensitive option. Free, browser-based.",
    longDescription: "Sorting lines of text alphabetically is useful for organising lists, normalising configuration files, sorting word lists, and preparing data for comparison. This tool sorts lines in ascending (A→Z) or descending (Z→A) order, with options for case-sensitive comparison and whitespace trimming. Empty lines are moved to the end of the output.",
    primaryKeyword: "sort lines online",
    category: "text-tools",
    howTo: [
      "Paste the text with lines you want to sort into the input textarea.",
      "Choose the sort direction (A→Z or Z→A) and options, then click 'Sort Lines'.",
      "The sorted text appears in the output. Copy or download as needed.",
    ],
    faqs: [
      { question: "What sort algorithm is used?", answer: "The tool uses JavaScript's built-in Array.sort() with a locale-aware comparison function (localeCompare). This produces correct alphabetical ordering for English and most European languages, handling accented characters (é, ü, ñ) in the expected alphabetical position rather than sorting them after 'z'." },
      { question: "How are numbers sorted?", answer: "Lines are sorted lexicographically (as text strings) by default. This means '10' sorts before '2' because '1' comes before '2' in string comparison. For numerically correct sorting of number lists, reorder the numbers to have consistent zero-padding (e.g., '02', '10') before sorting, or use a spreadsheet application that offers numeric sort." },
      { question: "What does 'case-sensitive' sorting mean?", answer: "With case-sensitive sorting enabled, uppercase letters sort before lowercase letters in ASCII order (A, B, C ... Z, a, b, c). With case-sensitive disabled (the default), upper and lowercase versions of the same letter sort together, giving the natural dictionary order (a, Apple, banana, Banana)." },
      { question: "Are empty lines preserved or removed?", answer: "Empty lines are preserved but sorted to the end of the output. If 'Trim whitespace' is enabled, lines that consist only of spaces or tabs are treated as empty lines and also sorted to the end. This keeps the data lines together at the top of the output." },
      { question: "Can I sort a list of file paths or URLs?", answer: "Yes. File paths and URLs are just strings and will be sorted alphabetically. This can be useful for normalising import lists in code files or organising URL lists. For file paths, the sort is by the full path string, so '/assets/a.js' sorts before '/components/a.js'." },
      { question: "Is my text sent to a server?", answer: "No. All sorting runs entirely in your browser using pure JavaScript. No data is sent to any server." },
    ],
  },
  {
    slug: "lorem-ipsum-generator",
    name: "Lorem Ipsum Generator",
    title: "Lorem Ipsum Generator — Free Placeholder Text Generator",
    description: "Generate Lorem Ipsum placeholder text online. Choose paragraphs, sentences or words. Free, instant.",
    longDescription: "Lorem Ipsum is the standard placeholder text used in graphic design, web development, and publishing to fill space before real content is available. This generator produces Lorem Ipsum text derived from the classic Cicero passage, in the quantity and format you specify — full paragraphs, individual sentences, or a specific number of words. The output is ready to copy and paste into mockups, templates, or design tools.",
    primaryKeyword: "lorem ipsum generator",
    category: "text-tools",
    howTo: [
      "Choose the type of output: Paragraphs, Sentences, or Words.",
      "Enter the quantity (e.g., 3 paragraphs, 5 sentences, 50 words).",
      "Click 'Generate' and copy the output to clipboard.",
    ],
    faqs: [
      { question: "What is Lorem Ipsum?", answer: "Lorem Ipsum is placeholder text derived from a passage in 'de Finibus Bonorum et Malorum' (On the Ends of Good and Evil), written by the Roman statesman and philosopher Cicero in 45 BC. The scrambled Latin has been used as placeholder text in typesetting since the 1500s. The text is used because it has a natural distribution of letters and words, making it look like readable prose from a distance, without distracting readers with actual content." },
      { question: "Is the generated text always the same?", answer: "This generator uses a fixed corpus of Lorem Ipsum words and a deterministic generation algorithm, so the same settings produce similar text. However, the generator cycles through the word list and varies sentence and paragraph lengths to produce more natural-looking output. Each generation is slightly varied in paragraph and sentence structure." },
      { question: "Why do designers use Lorem Ipsum instead of real text?", answer: "Real text distracts reviewers — people read the actual words and comment on the content rather than the design. Lorem Ipsum is meaningless to most readers, so they focus on the visual layout, typography, and hierarchy instead of the copy. It also allows designs to proceed before the final content is written, which is common in agile projects." },
      { question: "What is the difference between paragraph, sentence and word mode?", answer: "Paragraph mode generates complete paragraphs separated by blank lines, each containing 4–8 sentences. Sentence mode generates individual sentences separated by newlines — useful for filling form fields, captions, or card descriptions. Word mode generates the exact number of words requested as a single continuous block — useful for headline or tagline placeholders." },
      { question: "Can I start with 'Lorem ipsum dolor sit amet'?", answer: "Yes. The generator always starts the first paragraph with the classic opening 'Lorem ipsum dolor sit amet, consectetur adipiscing elit...' which is the traditionally recognised Lorem Ipsum opening. Subsequent paragraphs use other portions of the Lorem Ipsum corpus." },
      { question: "Is there a maximum amount I can generate?", answer: "You can generate up to 10 paragraphs, 50 sentences, or 1,000 words in a single generation. The generation is instant regardless of quantity, as it is pure JavaScript running in your browser." },
    ],
  },
  {
    slug: "diff-checker",
    name: "Diff Checker",
    title: "Diff Checker — Compare Two Texts Online",
    description: "Compare two pieces of text online and see the differences highlighted. Line-by-line diff. Free, browser-based.",
    longDescription: "The diff checker compares two texts line by line and highlights the differences: lines only in the original text are shown with a red background, lines only in the modified text are shown with a green background, and identical lines are shown in white. This is useful for comparing versions of code, configuration files, documents, or any text where you need to see what changed between two versions.",
    primaryKeyword: "diff checker online",
    category: "text-tools",
    howTo: [
      "Paste the original text into the left textarea and the modified text into the right textarea.",
      "Click 'Compare' to see the differences highlighted below.",
      "Red lines exist only in the original. Green lines exist only in the modified text.",
    ],
    faqs: [
      { question: "How does the line-by-line diff work?", answer: "The tool splits both texts into lines and uses a longest common subsequence (LCS) algorithm to identify matching lines. Lines present in the original but not the modified version are marked as removed (red). Lines present in the modified but not the original are marked as added (green). Lines that appear in both are marked as unchanged (grey). This is the same approach used by the Unix diff command and tools like git diff." },
      { question: "Is the comparison case-sensitive?", answer: "Yes. The comparison is case-sensitive by default — 'Hello' and 'hello' are treated as different lines. Leading and trailing whitespace differences are also detected. A line with trailing spaces is treated as different from the same line without trailing spaces." },
      { question: "Can I compare code files using this tool?", answer: "Yes. Paste the old version of a file in the left area and the new version in the right area. The diff will show exactly which lines changed. For code, this is similar to running git diff between two versions of a file. The tool works with any text format — code, configuration files, documentation, prose — as long as the content is line-oriented." },
      { question: "Does the tool support character-level diff?", answer: "This tool performs line-level diff only — it shows which lines are added, removed, or unchanged, but does not highlight which specific characters within a changed line are different. For character-level or word-level diff highlighting within changed lines, use a dedicated diff tool like git diff with the --word-diff flag, or a desktop application like Meld or Beyond Compare." },
      { question: "What is the maximum size of text I can compare?", answer: "There is no hard limit, but the LCS algorithm has O(n×m) complexity where n and m are the number of lines in each text. Very long texts (thousands of lines each) may take a second or two to compare. Texts with tens of thousands of lines will be noticeably slow. For large files, a command-line tool like diff or git diff is more appropriate." },
      { question: "Is my text sent to a server?", answer: "No. The diff algorithm runs entirely in your browser using pure JavaScript. No data is sent to any server. This makes the tool safe for comparing confidential documents, proprietary code, or sensitive configuration files." },
    ],
  },
];

export function getTextTool(slug: string): TextTool | undefined {
  return textTools.find((t) => t.slug === slug);
}

export function getAllTextTools(): TextTool[] {
  return textTools;
}
